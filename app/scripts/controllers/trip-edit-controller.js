'use strict';

App.TripEditController = Ember.ObjectController.extend({
  editMode: 'editRoute'

  // waypoints: function() {
  //   return this.currentModel.get('waypoints');
  // }.property('waypoints')
});